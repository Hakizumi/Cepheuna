package org.sempiria.cepheuna.tools;

/**
 * Base tool interface.Only implements this interface,tool can be scanned by tool registry.
 * It is just a markup interface and has no functional usage
 *
 * @since 1.0.0
 * @version 1.0.0
 * @author Sempiria
 */
public interface AgentTool {
}
